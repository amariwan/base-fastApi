[pytest]
addopts = -ra
testpaths = tests
pythonpath = app
asyncio_mode = auto
markers =
    unit: Schnelle, isolierte Unit-Tests ohne externe Ressourcen
    integration: Tests über mehrere Schichten (DB / Services / Dependencies)
    e2e: End-to-End Tests über die laufende FastAPI-App / HTTP-Routing
